spring:
  application:
    name: INSURANCE-REPORT-GENERATION
    
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/insurance_report_generation_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
      

    show-sql: true

  mvc:
    view:
      prefix: /pages/
      suffix: .jsp